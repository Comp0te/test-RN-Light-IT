import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  stars: {
    flexDirection: 'row',
  },
  title: {
    marginBottom: 5,
    fontSize: 20,
  }
});

export default style;