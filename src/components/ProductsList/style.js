import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  root: {
    flex: 1,
  },
  tabView: {
    flex: 1,
    paddingLeft: 0,
    paddingRight: 0,
  },
});

export default style;
